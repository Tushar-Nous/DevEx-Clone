name: ci
on: [push, pull_request]
jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install deps
        run: pip install -r requirements.txt || true
      - name: Unit tests
        run: echo "run unit tests here"
      - name: Prompt tests
        run: echo "run prompt tests here"
      - name: Regression tests
        run: echo "run regression tests here"
      - name: Bias checks
        run: echo "run bias checks here"
